<div class="report-insights">
  <!-- Volume Leader -->
  <div class="insight-card volume-leader-card" id="volume-leader-card">
    <div class="insight-label">VOLUME LEADER</div>
    <div class="insight-service" id="vl-service">-</div>
    <div class="insight-detail" id="vl-detail">-</div>
  </div>

  <!-- QR Value Advantage -->
  <div class="insight-card qr-advantage-card" id="qr-advantage-card">
    <div class="insight-label">QR VALUE ADVANTAGE</div>
    <div class="qr-avg-main">
      <div class="qr-avg-label">AVERAGE TRANSACTION VALUE</div>
      <div class="qr-avg-value" id="qr-avg-value">ETB -</div>
      <div class="qr-avg-sub">per transaction</div>
    </div>
    <div class="qr-comparisons" id="qr-comparisons">
      <!-- Populated by JavaScript -->
    </div>
  </div>

  <!-- Key Takeaway -->
  <div class="insight-card takeaway-card" id="takeaway-card">
    <div class="insight-label">KEY TAKEAWAY</div>
    <div class="takeaway-text" id="takeaway-text">-</div>
  </div>
</div>